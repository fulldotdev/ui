---
import { transform } from '@fullui/ui'
import { getEntry } from 'astro:content'

const index = await getEntry('pages', 'index')
const transformed = await transform(index)

console.log('transformed', transformed)
---
