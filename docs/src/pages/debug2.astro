---
import { transformCasing } from '@fullui/ui'

const obj = {
  helloWorld: 'hello-world',
  nest: {
    __hello__World: 'hello-world',
  },
  '_bye-world': 'hello-world',
}

const casing = transformCasing(obj)

console.log('CAMEL', casing)
---
