---
title: Section
description: Use this as the main wrapper for your full-width sections.
props:
  - name: structure
    type: 'stack | split | spread | group'
    default: stack
  - name: frame
    type: 'fill | viewport | panel'
  - name: variant
    type: fill | subtle | outline | soft | solid | surface
  - name: size
    type: 'sm | md | lg'
  - name: compact
    type: attribute
  - name: theme
    type: string
  - name: text
    type: string
  - name: html
    type: string
  - name: as
    type: HTMLTag
    default: section
  - name: HTML Attributes
    type: Polymorphic<as>
---

import PropsTable from '../../../components/PropsTable.astro'
import SizeProp from '../../props/SizeProp.mdx'
import VariantProp from '../../props/VariantProp.mdx'
import SplitProp from '../../props/structures/Split.mdx'
import SpreadProp from '../../props/structures/Spread.mdx'
import PanelProp from '../../props/frames/Panel.mdx'

{frontmatter.description}

<br />

:::note
Since the Section component is meant to be used as a full-width block, it cannot be previewed here in the docs. Please see the [homepage](/) for examples.
:::

## Props

<PropsTable props={frontmatter.props} />

## Examples

### `frame`

If set to `viewport` the section has a minimum height of 100vh.

If set to `panel` margins are added around certain child elements and backgrounds and borders are applied according to your [color variables](/overview/theming). You can use the variant prop explained below to adjust these styles.

#### `panel`

<PanelProp />

### `variant`

<VariantProp />

### `size`

<SizeProp />

### `structure`

Applies all **styles & props** of the chosen `Structure` component to this component.

#### `split`

<SplitProp />

#### `spread`

<SpreadProp />
