---
# TODO docs fixed, worden geen goede voorbeelden gebruikt. De bdage wordt gebruikt als een soort tagline boven de Heading
title: Badge
description: Displays a Badge or a component that looks like a Badge.
sidebar: 
  hidden: false
---
import AsPolymorhpic from '../globalprops/tag/asPolymorphic.mdx'
import IfAny from '../globalprops/tag/ifAny.mdx'
import Theme from '../globalprops/tag/theme.mdx'
import Size from '../globalprops/tag/size.mdx'
import Compact from '../globalprops/tag/compact.mdx'

import KeyComponentName from '../globalprops/children/keyComponentName.mdx'
import ValueElementName from '../globalprops/children/valueElementName.mdx'
import ValueComponentName from '../globalprops/children/valueComponentName.mdx'

### Props
Extends [Tag](/foundation/tag), [Children](/foundation/children)
```ts
variant?: 'solid' | 'soft' | 'surface' | 'outline'
```

### Defaults
```ts
as = 'span'
variant = 'solid'
```

## Examples
The examples below could also be written with [different syntax](/overview/components).

### Variants
```astro live
---
import { Badge } from '@fullui/ui'
---

<Badge variant="solid">Badge</Badge>
<Badge variant="soft">Badge</Badge>
<Badge variant="surface">Badge</Badge>
<Badge variant="outline">Badge</Badge>
```

### Color
```astro live
---
import { Badge } from '@fullui/ui'
---

<Badge color="accent" variant="solid">Badge</Badge>
<Badge color="accent" variant="soft">Badge</Badge>
<Badge color="accent" variant="surface">Badge</Badge>
<Badge color="accent" variant="outline">Badge</Badge>
```

### Contrast
```astro live
---
import { Badge } from '@fullui/ui'
---

<Badge contrast variant="solid">Badge</Badge>
<Badge contrast variant="soft">Badge</Badge>
<Badge contrast variant="surface">Badge</Badge>
<Badge contrast variant="outline">Badge</Badge>
```

### Icon
```astro live
---
import { Badge } from '@fullui/ui'
import { Icon } from '@fullui/ui'
---

<Badge icon={{name: "arrow-left"}} html="Badge" />
<Badge html="Badge" icon={{name: "arrow-right"}} />
```

### Size
```astro live
---
import { Badge } from '@fullui/ui'
---

<Badge size="xl"> Badge </Badge>
<Badge size="lg"> Badge </Badge>
<Badge size="md"> Badge </Badge>
<Badge size="sm"> Badge </Badge>
<Badge size="xs"> Badge </Badge>
```

### Compact
Why size and compact? Read [the page about sizes](/theme/size).
```astro live
---
import { Badge } from '@fullui/ui'
---

<Badge size="xl" compact> Badge </Badge>
<Badge size="lg" compact> Badge </Badge>
<Badge size="md" compact> Badge </Badge>
<Badge size="sm" compact> Badge </Badge>
<Badge size="xs" compact> Badge </Badge>
```

## Global Props
### `as` (Polymorphic)
<AsPolymorhpic />

### `if`
<IfAny />

### `theme`
<Theme />

### `size`
<Size />

### `compact`
<Compact />

### If `key` ends with a component name
<KeyComponentName />

### If `value` contains a `as` key with an HTML tag name
<ValueElementName />

### If `value` contains a `component` key with a component name
<ValueComponentName />