---
title: Table
description: A native, styled table element.
props:
  table:
    - name: header
      type: ComponentProps<typeof TableHeader> || ComponentProps<typeof TableHeader>['cells']
      value: ['Product', 'Color', 'Quantity']
    - name: body
      type: ComponentProps<typeof TableBody> || ComponentProps<typeof TableBody>['rows']
      value: [['Apple', 'Green', '2'], ['Banana', 'Yellow', '3']]
    - name: footer
      type: ComponentProps<typeof TableFooter> || ComponentProps<typeof TableFooter>['cells']
      value: ['Total', '&zwnj;', '2']
    - name: size
      type: sm | md | lg
    - name: color
      type: 'base | brand'
    - name: scheme
      type: light | dark
    - name: text
      type: string
    - name: html
      type: string
    - name: HTML Attributes
      type: HTMLAttributes<table>
  tableHeader:
    - name: cells
      type: ComponentProps<typeof TableHead> | ComponentProps<typeof TableHead>['html']
    - name: HTML Attributes
      type: HTMLAttributes<thead>
  tableBody:
    - name: rows
      type: ComponentProps<typeof TableRow> | ComponentProps<typeof TableRow>['cells']
    - name: HTML Attributes
      type: HTMLAttributes<tbody>
  tableRow:
    - name: cells
      type: ComponentProps<typeof TableCell> | ComponentProps<typeof TableCell>['text']
    - name: HTML Attributes
      type: HTMLAttributes<tr>
  tableCell:
    - name: text
      type: string
    - name: html
      type: string
    - name: HTML Attributes
      type: HTMLAttributes<td>
  tableHead:
    - name: text
      type: string
    - name: html
      type: string
    - name: HTML Attributes
      type: HTMLAttributes<th>
  tableFooter:
    - name: cells
      type: ComponentProps<typeof TableCell> | ComponentProps<typeof TableCell>['text']
    - name: HTML Attributes
      type: HTMLAttributes<tfoot>
---

import PropsTable from '../../../components/PropsTable.astro'
import SizeProp from '../../props/SizeProp.mdx'
import Story from '../../../components/Story.astro'

{frontmatter.description}

<br />

<Story title={frontmatter.title} props={frontmatter.props.table} />

```astro live
---
import { Table } from 'fulldev-ui'
---

<Table
  header={['Product', 'Color', 'Quantity']}
  body={[
    ['Apple', 'Green', '2'],
    ['Banana', 'Yellow', '3'],
  ]}
  footer={[{ text: 'Total', colspan: '2' }, '5']}
/>
```

## Props

The `Table` component exists of multiple sub-components. Each sub-component has its own set of props.

#### `<Table>`

<PropsTable props={frontmatter.props.table} />

#### `<TableHeader>`

<PropsTable props={frontmatter.props.tableHeader} />

#### `<TableBody>`

<PropsTable props={frontmatter.props.tableBody} />

#### `<TableRow>`

<PropsTable props={frontmatter.props.tableRow} />

#### `<TableCell>`

<PropsTable props={frontmatter.props.tableCell} />

#### `<TableFooter>`

<PropsTable props={frontmatter.props.tableFooter} />

## Examples

### `size`

<SizeProp />

```astro live
---
import { Table } from 'fulldev-ui'

const table = {
  header: ['Product', 'Quantity'],
  body: [['Apple', '2']],
}
---

<Table size="sm" {...table} />
<Table size="md" {...table} />
<Table size="lg" {...table} />
```

### Props vs slots

```astro
<Table>
  <TableHeader cells={['First Name', 'Last Name', 'Age']} />
  <TableBody>
    <TableRow cells={['John', 'Doe', '31']} />
    <TableRow cells={['Jane', 'Doe', '29']} />
  </TableBody>
  <TableFooter cells={[{ text: 'Total', colspan: '2' }, '2']} />
</Table>
```

instead of creating each row with a `TableRow` component, you can use the `rows` prop in the `TableBody` component.

```astro
<TableBody
  rows={[
    ['John', 'Doe', '31'],
    ['Jane', 'Doe', '29'],
  ]}
/>
```
