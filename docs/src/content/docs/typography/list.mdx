---
title: List
description: A native, styled list element with list items inside.
props:
  list:
    - name: items
      type: string[]
      value: ['Item 1', 'Item 2', 'Item 3']
    - name: size
      type: xs | sm | md | lg | xl
    - name: color
      type: base | accent
    - name: contrast
      type: boolean
    - name: as
      type: HTMLTag
      default: ul
    - name: HTML Attributes
      type: Polymorphic<as>
  listItem:
    - name: html
      type: string
    - name: text
      type: string
    - name: as
      type: HTMLTag
      default: li
    - name: HTML Attributes
      type: Polymorphic<as>
---

import PropsTable from '../../../components/PropsTable.astro'
import SizeProp from '../../props/SizeProp.mdx'
import VariantProp from '../../props/VariantProp.mdx'
import Story from '../../../components/Story.astro'

{frontmatter.description}

<br />

<Story title={frontmatter.title} props={frontmatter.props.list} />

## Props

### `<List>`

<PropsTable props={frontmatter.props.list} />

### `<ListItem>`

<PropsTable props={frontmatter.props.listItem} />

## Examples

### `items`

```astro live
---
import { List } from 'fulldev-ui'
---

<List items={['Item 1', { text: 'Item 2' }]} />
```

### `size`

<SizeProp />

```astro live
---
import { List } from 'fulldev-ui'
---

<List size="sm" items={['Item 1', 'Item 2']} />
<List size="md" items={['Item 1', 'Item 2']} />
<List size="lg" items={['Item 1', 'Item 2']} />
```

### `color`

<VariantProp />

```astro live
---
import { List } from 'fulldev-ui'
---

<List color="base" items={['Item 1', 'Item 2']} />
<List color="brand" items={['Item 1', 'Item 2']} />
```

### `contrast`

Set to `true` by default on Headings, which you can override by setting it to `false`.

```astro live
---
import { List } from 'fulldev-ui'
---

<List contrast={false} items={['Item 1', 'Item 2']} />
```
