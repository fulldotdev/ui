---
title: Buttons
sidebar: 
  order: 6
---

## Props
extends [Endomorphic](/guides/components), [Tag](/components/tag), [Button](/components/button)
```ts
interface Props
  extends Endomorphic<'buttons', Props, ComponentProps<typeof Button>[]>,
    ComponentProps<typeof Tag> {
  index?: number | null
}
```

## Example
Buttons will automatically fade from `solid` to `soft` and then `ghost`.
```astro live
---
import { Buttons } from '@fulldevlabs/fullui'

const myButtons = [
  {"button": "First Button"},
  {"button": "Second Button"},
  {"button": "Third Button"},
]
---

<Buttons buttons={myButtons} />
```