---
import { Table } from 'fulldev-ui'

const { props } = Astro.props
---

<Table
  class:list={'not-content'}
  size="md"
  header={['Prop', 'Type', 'Default']}
  body={props.map((item: any) => [
    item.prop,
    item.type,
    item.default || `&mdash;`,
  ])}
/>
