---
import { getCollection, getEntryBySlug } from 'astro:content'
const entry = await getEntryBySlug('pages', '_pages')
const entries = await getCollection('pages')
---

<!doctype html>
<html lang="en">
  <head> </head>
  <body> </body>
</html>
