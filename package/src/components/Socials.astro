<!-- ---
import type { ComponentProps,HTMLTag } from 'astro/types'
import { endomorphic,type Endomorphic } from '../utils'
import Social from './Socials.astro'
import Tag from './Tag.astro'

type Props<As extends HTMLTag = 'div'> = Endomorphic<
  'socials',
  ComponentProps<typeof Tag<As>> & {
    socials?: ComponentProps<typeof Social>[]
    never?: never // FIXME see endomorphic.ts for explanation
  }
>

const { socials, ...rest } = endomorphic(Astro.props, 'socials')
---

{socials?.map((social) => <Social {...social} />)} -->
