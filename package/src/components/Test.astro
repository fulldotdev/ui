---
import { z } from 'zod';
import { componentSchema } from '../schemas/componentSchema';

type Props = z.infer<typeof testSchema>;

export const testSchema = componentSchema({
  hoi: z.string(),
});

const props = testSchema.parse(Astro.props);
// console.log(props.hoi)
---
