---
import type { ComponentProps } from 'astro/types'
import { buildProps, type BuildProps } from '../utils'
import Image from './Image.astro'
import Tag from './Tag.astro'

// type Props<As extends HTMLTag = 'a'> = BuildProps<
//   'logo',
//   ComponentProps<typeof Tag<As>> &
//     ComponentProps<typeof Image> & {
//       logo?: ComponentProps<typeof Image>['image']
//     }
// >

interface Props
  extends BuildProps<'logo', Props, ComponentProps<typeof Image>['image']>,
    ComponentProps<typeof Tag>,
    ComponentProps<typeof Image> {}

const { as = 'a', ...rest } = buildProps('logo', Astro.props)
---

<Tag
  class="logo"
  href="/"
  {as}
  {...rest}
>
  <Image {...rest} />
  <slot />
</Tag>

<style is:global>
  .logo {
    .image {
      display: block !important;
      width: auto !important;
      height: var(--space-5) !important;
      object-fit: contain !important;
      color: var(--hue12) !important;
    }
  }
</style>
