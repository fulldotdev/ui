---
import { z } from 'astro:content'
import Tag, { tag } from './Tag.astro'

type Props = z.infer<typeof title>

export const title = tag.extend({})

const { as = 'h2', contrast = true, ...rest } = title.parse(Astro.props)
---

<Tag
  class:list={['title']}
  {as}
  {contrast}
  {...rest}
>
  <slot />
</Tag>

<style is:global>
  .title {
    color: var(--hue11);
    font: var(--font);
    font-weight: 700;
    font-size: var(--text-3);
    line-height: 1.25;

    &.contrast {
      color: var(--hue12);
    }
  }
</style>
