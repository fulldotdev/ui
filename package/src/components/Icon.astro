---
import tablerIcons from '@iconify-json/tabler/icons.json'
import { z } from 'zod'
import { componentSchema } from '../schemas/componentSchema'
import Component from './misc/Component.astro'

type Props = z.infer<typeof iconSchema>

export const iconSchema = componentSchema({
  name: z.string(),
})

const { tag = 'i', name, ...rest } = iconSchema.parse(Astro.props)
---

{
  name && (
    <Component
      class:list={['icon']}
      {tag}
      {...rest}
    >
      {name && (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          set:html={(tablerIcons as any)?.icons[name as any]?.body}
        />
      )}
    </Component>
  )
}

<style is:global>
  .icon {
    display: inline-flex;
    position: relative;
    justify-content: center;
    align-items: center;
    width: 1em;
    height: 1em;
    color: var(--hue11);
    font-size: var(--text-2);

    &.contrast {
      color: var(--hue12);
    }

    /* FIXME */
    &:is(.display > .icon) {
      width: 1.6em;
      height: 1.6em;
      color: var(--hue9);
    }

    /* FIXME */
    &:is(.button > .icon),
    &:is(.badge > .icon),
    &:is(.link > .icon) {
      color: inherit;
    }

    svg {
      position: absolute;
      width: 120%;
      height: 120%;
    }
  }
</style>
