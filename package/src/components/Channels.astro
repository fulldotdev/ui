<!-- ---
import type { ComponentProps,HTMLTag } from 'astro/types'
import { buildProps,type BuildProps } from '../utils'
import Channel from './Channel.astro'
import Tag from './Tag.astro'

type Props<As extends HTMLTag> = BuildProps<
  'channels',
  ComponentProps<typeof Tag<As>> & {
    channels?: ComponentProps<typeof Channel>[]
    never?: never // FIXME see buildProps.ts for explanation
  }
>

const { channels, ...rest } = buildProps('channels', Astro.props)
---

{channels?.map((channel) => <Channel {...channel} />)} -->
