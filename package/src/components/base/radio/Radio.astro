---
import RadioField from './RadioField.astro'

type Props = {
  color?: 'base' | 'brand'
  label?: string
  name?: string
  options?: string[]
}

const { label, name, options, ...rest } = Astro.props
---

<RadioField
  label={label}
  name={name}
  options={options}
  {...rest}
/>
