---
import type { Polymorphic } from 'astro/types'

type Props<As extends 'small'> = Polymorphic<{ as: As }> & {
  text?: string
}

const { text, ...rest } = Astro.props
---

<small>{text}<slot /></small>
