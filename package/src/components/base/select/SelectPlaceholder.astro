---
import type { ComponentProps } from 'astro/types'
import SelectOption from './SelectOption.astro'

type Props = ComponentProps<typeof SelectOption>

const { text, ...rest } = Astro.props
---

<SelectOption
  disabled
  selected
  {...rest}
>
  <slot />
</SelectOption>
