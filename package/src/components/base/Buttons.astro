---
import type { ComponentProps } from 'astro/types'
import Button from '../base/Button.astro'
import Plural from './_Plural.astro'

type Props = ComponentProps<typeof Button> & {
  [key: number]: ComponentProps<typeof Button>
}

const { props } = Astro
---

<Plural
  component={Button}
  {props}
/>
