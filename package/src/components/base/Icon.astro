---
import tablerIcons from '@iconify-json/tabler/icons.json'
import { z } from 'zod'
import { componentSchema } from '../../schemas/componentSchema'
import Tag from '../utilities/Tag.astro'

type Props = z.infer<typeof iconSchema>

export const iconSchema = componentSchema({
  name: z.string(),
})

const { as = 'i', name, ...rest } = iconSchema.parse(Astro.props)
---

{
  name && (
    <Tag
      class:list={['full-icon']}
      {as}
      {...rest}
    >
      {name && (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          set:html={(tablerIcons as any)?.icons[name as any]?.body}
        />
      )}
    </Tag>
  )
}

<style is:global>
  .full-icon {
    display: inline-flex;
    position: relative;
    justify-content: center;
    align-items: center;
    width: 1em;
    height: 1em;
    color: var(--hue11);
    font-size: var(--text-2);

    &.contrast {
      color: var(--hue12);
    }

    /* FIXME */
    &:is(.full-display > .icon) {
      width: 1.6em;
      height: 1.6em;
      color: var(--hue9);
    }

    /* FIXME */
    &:is(.full-button > .icon),
    &:is(.full-badge > .icon),
    &:is(.full-link > .icon) {
      color: inherit;
    }

    svg {
      position: absolute;
      width: 120%;
      height: 120%;
    }
  }
</style>
