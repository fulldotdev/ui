---
// TODO re-ecport astro-icon package
import tablerIcons from '@iconify-json/tabler/icons.json'
import Tag from '../utility/Tag.astro'

type Props = any

const { as = 'i', name, ...rest } = Astro.props
---

{
  name && (
    <Tag
      class:list={['full-icon']}
      {as}
      {...rest}
    >
      {name && (
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 24 24"
          set:html={(tablerIcons as any)?.icons[name as any]?.body}
        />
      )}
    </Tag>
  )
}

<style is:global>
  .full-icon {
    display: inline-flex;
    position: relative;
    justify-content: center;
    align-items: center;
    width: 1em;
    height: 1em;
    color: var(--hue11);
    font-size: var(--text-2);

    &.contrast {
      color: var(--hue12);
    }

    /* FIXME */
    &:is(.full-button > .icon),
    &:is(.full-badge > .icon),
    &:is(.full-link > .icon) {
      color: inherit;
    }

    svg {
      position: absolute;
      width: 120%;
      height: 120%;
    }
  }
</style>
