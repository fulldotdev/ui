---
import type { ComponentProps } from 'astro/types'
import { endomorphic, type Endomorphic } from '../utils'
import Tag from './Tag.astro'

interface Props
  extends Endomorphic<Props, 'description', string | null>,
    ComponentProps<typeof Tag> {}

const {
  as = 'p',
  description,
  ...rest
} = endomorphic(Astro.props, 'description')
---

<Tag
  class:list={['description']}
  {as}
  {...rest}
>
  <Fragment set:html={description} />
  <slot />
</Tag>

<style is:global>
  .description {
    color: var(--hue11);
    font: var(--font);

    &.contrast {
      color: var(--hue12);
    }
  }
</style>
