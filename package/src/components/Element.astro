---
import { componentSchema } from '../schemas/componentSchema'
import Children from './Children.astro'
import Tag from './Tag.astro'

export const elementSchema = componentSchema({})

const { ...rest } = elementSchema.parse(Astro.props)
---

<Tag {...rest}>
  <Children {...rest} />
  <slot />
</Tag>
