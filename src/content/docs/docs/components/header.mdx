---
title: Header
description: A layout component for the top section of a page.
---

```astro live
---
import { Button } from "@/components/ui/button"
import { Header, HeaderActions, HeaderContent } from "@/components/ui/header"
import { Logo, LogoImage, LogoText } from "@/components/ui/logo"
---

<Header>
  <HeaderContent>
    <Logo href="/">
      <LogoText>fulldev/ui</LogoText>
    </Logo>
    <HeaderActions class="ml-auto">
      <Button size="sm" variant="ghost">Sign In</Button>
      <Button size="sm">Sign Up</Button>
    </HeaderActions>
  </HeaderContent>
</Header>
```

## Installation

To install the header component, run the following command:

```bash
pnpm dlx shadcn@latest add @fulldev-ui/header
```

## Usage

```ts
import { Button } from "@/components/ui/button"
import { Header, HeaderActions, HeaderContent } from "@/components/ui/header"
import { Logo, LogoImage, LogoText } from "@/components/ui/logo"```

```astro
<Header>
  <HeaderContent>
    <Logo href="/">
      <LogoText>fulldev/ui</LogoText>
    </Logo>
    <HeaderActions class="ml-auto">
      <Button size="sm" variant="ghost">Sign In</Button>
      <Button size="sm">Sign Up</Button>
    </HeaderActions>
  </HeaderContent>
</Header>
```

## Examples

### Outline

```astro live
---
import { Button } from "@/components/ui/button"
import { Header, HeaderActions, HeaderContent } from "@/components/ui/header"
import { Logo, LogoImage, LogoText } from "@/components/ui/logo"
---

<Header variant="ghost">
  <HeaderContent>
    <Logo href="/">
      <LogoText>fulldev/ui</LogoText>
    </Logo>
    <HeaderActions class="ml-auto">
      <Button size="sm" variant="ghost">Sign In</Button>
      <Button size="sm">Sign Up</Button>
    </HeaderActions>
  </HeaderContent>
</Header>
```

### Muted

```astro live
---
import { Button } from "@/components/ui/button"
import { Header, HeaderActions, HeaderContent } from "@/components/ui/header"
import { Logo, LogoImage, LogoText } from "@/components/ui/logo"
---

<Header variant="muted">
  <HeaderContent>
    <Logo href="/">
      <LogoText>fulldev/ui</LogoText>
    </Logo>
    <HeaderActions class="ml-auto">
      <Button size="sm" variant="ghost">Sign In</Button>
      <Button size="sm">Sign Up</Button>
    </HeaderActions>
  </HeaderContent>
</Header>
```

### Float

```astro live
---
import { Button } from "@/components/ui/button"
import { Header, HeaderActions, HeaderContent } from "@/components/ui/header"
import { Logo, LogoImage, LogoText } from "@/components/ui/logo"
---

<Header frame="float">
  <HeaderContent>
    <Logo href="/">
      <LogoText>fulldev/ui</LogoText> 
    </Logo>
    <HeaderActions class="ml-auto">
      <Button size="sm" variant="ghost">Sign In</Button>
      <Button size="sm">Sign Up</Button>
    </HeaderActions>
  </HeaderContent>
</Header>
```

### Float muted

```astro live
---
import { Button } from "@/components/ui/button"
import { Header, HeaderActions, HeaderContent } from "@/components/ui/header"
import { Logo, LogoImage, LogoText } from "@/components/ui/logo"
---

<Header frame="float" variant="muted">
  <HeaderContent>
    <Logo href="/">
      <LogoText>fulldev/ui</LogoText> 
    </Logo>
    <HeaderActions class="ml-auto">
      <Button size="sm" variant="ghost">Sign In</Button>
      <Button size="sm">Sign Up</Button>
    </HeaderActions>
  </HeaderContent>
</Header>
```