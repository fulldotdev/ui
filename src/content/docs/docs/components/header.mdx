---
title: Header
description: A layout component for the top section of a page.
sidebar:
  badge:
    text: ⋅
    variant: success
---

import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
import { Icon } from "@/components/ui/icon"

<Alert class="not-content">
  <Icon name="alert-circle" />
  <AlertTitle>Work in progress</AlertTitle>
  <AlertDescription>
    This header component is a work in progress. To see it in action, have a
    look at:
    <a class="text-primary inline" href="/blocks/header/">
      /blocks/header/
    </a>
  </AlertDescription>
</Alert>

## Installation

To install the header component, run the following command:

```bash
npx shadcn@latest add @fulldev/header
```

## Usage

```ts
import { Button } from "@/components/ui/button"
import { Header, HeaderActions, HeaderContent } from "@/components/ui/header"
import { Logo, LogoImage, LogoText } from "@/components/ui/logo"
```

```astro
<Header>
  <HeaderContent>
    <Logo href="/">
      <LogoText>fulldev/ui</LogoText>
    </Logo>
    <HeaderActions class="ml-auto">
      <Button size="sm" variant="ghost">Sign In</Button>
      <Button size="sm">Sign Up</Button>
    </HeaderActions>
  </HeaderContent>
</Header>
```

## Examples

### Floating

```astro live
---
import { Button } from "@/components/ui/button"
import { Header, HeaderActions, HeaderContent } from "@/components/ui/header"
import { Logo, LogoImage, LogoText } from "@/components/ui/logo"
---

<Header variant="floating">
  <HeaderContent>
    <Logo href="/">
      <LogoText>fulldev/ui</LogoText>
    </Logo>
    <HeaderActions class="ml-auto">
      <Button size="sm" variant="ghost">Sign In</Button>
      <Button size="sm">Sign Up</Button>
    </HeaderActions>
  </HeaderContent>
</Header>
```
