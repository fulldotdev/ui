---
title: Category
description: A reference page in my new Starlight docs site.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';
import CategorySection from '../../../components/blocks/categories/CategorySection.astro'
import CategoryDeck from '../../../components/blocks/categories/CategoryDeck.astro'
import CategoryCard from '../../../components/blocks/categories/CategoryCard.astro'
import CategoryLayout from '../../../components/blocks/categories/CategoryLayout.astro'


The `Category` component is used to render one or more categories, typically used in ecommerce websites.

Most of the time you will only use the `Section`, although it is possible to use each separately.

## CategorySection
{/* @dylan bla bla klinkt alsof hier opvulling moet komnen. ALs iets alleen opvulling is, laat het dat weg. Opvulling hebben we niks aan en hoe minder woorden hoe beter */}
bla bla

### Props

```ts
interface Props
  extends ComponentProps<typeof CategoryDeck>,
    Pick<
      Omit<ComponentProps<typeof Section>, 'collection'>,
      'tagline' | 'heading' | 'text' | 'buttons'
    > {}

const { props } = Astro
```

{/* @dylan in onderstaand voorbeeld - en dit geldt ook voor ALLE documentatie pagina's - doe het volgende */}
{/* Wanneer je een voorbeeld geeft van hoe je het gebruik, laat ook ALTIJD de imports zien. Dus: */}
{/* 
---
import component hier...
---

<Component/> gebruik component hier 
*/}


### Example code
<Tabs>
  <TabItem label="Self-closing">
    ```js
    <CategorySection text="Test" />
    ```
  </TabItem>
  <TabItem label="Slot">
    ```js
    <CategorySection>
      Test
    </CategorySection>
    ```
  </TabItem>
</Tabs>

<Window>
  <CategorySection text="Test" />
</Window>

## CategoryDeck
bla bla

### Props
```ts
interface Props extends Pick<ComponentProps<typeof Deck>, 'deck'> {
  categories?: ComponentProps<typeof CategoryCard>[] | null
}

const { props } = Astro
```

### Example code
<Tabs>
  <TabItem label="Self-closing">
    ```js
    <CategoryDeck text="Test" />
    ```
  </TabItem>
  <TabItem label="Slot">
    ```js
    <CategoryDeck>
      Test
    </CategoryDeck>
    ```
  </TabItem>
</Tabs>

<Window>
  <CategoryDeck text="Test" />
</Window>

## CategoryCard
bla bla

### Props
```ts
interface Props
  extends Pick<
    ComponentProps<typeof Card>,
    'href' | 'image' | 'tagline' | 'heading' | 'text'
  > {}

const { props } = Astro
```

{/* @dylan hier (in de blocks) zijn tabs denk ik niet nodig, als ze een custom component willen dan gebruiken ze namelijk wel het reguliere Card component, ipv CategoryCard */}
{/* Dit geldt voor alle blocks. Betekent dus: 1 voorbeeld met props, geen slots */}
### Example code
<Tabs>
  <TabItem label="Self-closing">
    ```js
    <CategoryCard text="Test" />
    ```
  </TabItem>
  <TabItem label="Slot">
    ```js
    <CategoryCard>
      Test
    </CategoryCard>
    ```
  </TabItem>
</Tabs>

<Window>
  <CategoryCard text="Test" />
</Window>

## CategoryLayout
bla bla

### Props
```ts
interface Props {
  entry: CollectionEntry<'categories'>
  frontmatter: CollectionEntry<'categories'>['data']
}

const { entry, frontmatter } = Astro.props
```

### Example code
<Tabs>
  <TabItem label="Self-closing">
    ```js
    <CategoryLayout text="Test" />
    ```
  </TabItem>
  <TabItem label="Slot">
    ```js
    <CategoryLayout>
      Test
    </CategoryLayout>
    ```
  </TabItem>
</Tabs>

<Window>
  <CategoryLayout text="Test" />
</Window>