---
title: Image
description: A reference page in my new Starlight docs site.
---

import Showcase from '../../../components/elements/Showcase.astro'
import Image from '../../../components/elements/image/Image.astro'

The `Image` element is used, as the name implies, to render a single image.

## Props
```ts
interface Props {
  media?: 'relative' | 'background'
  layout?: 'left' | 'center' | 'split' | 'wide'
  area?: 'fill' | 'panel' | 'screen'
  src?: string
  alt?: HTMLAttributes<'img'>['alt']
  loading?: HTMLAttributes<'img'>['loading']
  sizes?: {
    base: string
    sm?: string
    md?: string
    lg?: string
    xl?: string
    '2xl'?: string
  }
}

const {
  media = 'relative',
  area = 'fill',
  src,
  alt = '',
  loading = 'lazy',
  sizes = {
    base: '100vw',
  },
} = Astro.props
```

| Prop | Default | Purpose |
| ----------- | ----------- | ----------- |
| media | relative | Where the image will be rendered |
| area | fill | What area of the screen the image fills up |
| src | None | Directory of the image |
| alt | None | Text shown if the image does not load |
| loading | lazy | Loading method |
| sizes | 100vw | The sizes to use for each step |

## Example code

```js title="index.astro"
<Image src="/example.jpg" />
```

<Showcase>
  <Image src="/example.jpg" />
</Showcase>

