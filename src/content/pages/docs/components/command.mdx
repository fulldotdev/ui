---
type: docs
title: Command
description: Command menu for search and quick actions.
---

```astro live
---
import CalendarIcon from "lucide-static/icons/calendar.svg"
import CalculatorIcon from "lucide-static/icons/calculator.svg"
import SearchIcon from "lucide-static/icons/search.svg"
import SettingsIcon from "lucide-static/icons/settings.svg"

import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
} from "@/components/ui/command"
---

<Command class="h-[24.5rem] w-full max-w-md ring-1 ring-foreground/10">
  <CommandInput placeholder="Type a command or search..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>
        <CalendarIcon />
        Calendar
      </CommandItem>
      <CommandItem>
        <SearchIcon />
        Search Emoji
      </CommandItem>
      <CommandItem>
        <CalculatorIcon />
        Calculator
      </CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Settings">
      <CommandItem>
        <SettingsIcon />
        Profile
      </CommandItem>
      <CommandItem>
        <SettingsIcon />
        Billing
      </CommandItem>
      <CommandItem>
        <SettingsIcon />
        Settings
      </CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

## About

The `Command` components in this repo match the visual styling of the shadcn/ui
command menu, while using `@data-slot/command` for filtering, ranking, keyboard
navigation, and selection.

## Installation

### Command

This component is not yet available as a published `@fulldev/*` registry item.
Use the manual installation steps below.

### Manual

1. Install the dependency:

```bash
npm install @data-slot/command
```

2. Copy the files from `src/components/ui/command/` into your project.

3. Update the import paths to match your project setup.

## Usage

```ts
import {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from "@/components/ui/command"
```

```astro
<Command class="max-w-sm rounded-lg ring-1 ring-foreground/10">
  <CommandInput placeholder="Type a command or search..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>Calendar</CommandItem>
      <CommandItem>Search Emoji</CommandItem>
      <CommandItem>Calculator</CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Settings">
      <CommandItem>Profile</CommandItem>
      <CommandItem>Billing</CommandItem>
      <CommandItem>Settings</CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

## Composition

Use the following composition to build a `Command`:

```text
Command
├── CommandInput
└── CommandList
    ├── CommandEmpty
    ├── CommandGroup
    │   ├── CommandItem
    │   └── CommandItem
    ├── CommandSeparator
    └── CommandGroup
        ├── CommandItem
        └── CommandItem
```

## Examples

### Basic

A simple command menu in a dialog.

```astro live
---
import CalendarIcon from "lucide-static/icons/calendar.svg"
import CalculatorIcon from "lucide-static/icons/calculator.svg"
import SearchIcon from "lucide-static/icons/search.svg"

import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog"
import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
} from "@/components/ui/command"
---

<Dialog>
  <DialogTrigger variant="outline">Open Command Menu</DialogTrigger>
  <DialogContent
    class="top-1/3 translate-y-0 overflow-hidden rounded-xl! p-0"
    showCloseButton={false}
  >
    <DialogHeader class="sr-only">
      <DialogTitle>Command Palette</DialogTitle>
      <DialogDescription>Search for a command to run...</DialogDescription>
    </DialogHeader>
    <Command>
      <CommandInput placeholder="Type a command or search..." />
      <CommandList>
        <CommandEmpty>No results found.</CommandEmpty>
        <CommandGroup heading="Suggestions">
          <CommandItem>
            <CalendarIcon />
            Calendar
          </CommandItem>
          <CommandItem>
            <SearchIcon />
            Search Emoji
          </CommandItem>
          <CommandItem>
            <CalculatorIcon />
            Calculator
          </CommandItem>
        </CommandGroup>
      </CommandList>
    </Command>
  </DialogContent>
</Dialog>
```

### Shortcuts

```astro live
---
import KeyboardIcon from "lucide-static/icons/keyboard.svg"
import MailIcon from "lucide-static/icons/mail.svg"
import RocketIcon from "lucide-static/icons/rocket.svg"
import SettingsIcon from "lucide-static/icons/settings.svg"

import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandShortcut,
} from "@/components/ui/command"
---

<Command class="w-full max-w-md ring-1 ring-foreground/10">
  <CommandInput placeholder="Search shortcuts..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Quick actions">
      <CommandItem>
        <MailIcon />
        Send email
        <CommandShortcut>⌘E</CommandShortcut>
      </CommandItem>
      <CommandItem>
        <RocketIcon />
        Deploy project
        <CommandShortcut>⌘D</CommandShortcut>
      </CommandItem>
      <CommandItem>
        <SettingsIcon />
        Open settings
        <CommandShortcut>⌘,</CommandShortcut>
      </CommandItem>
      <CommandItem>
        <KeyboardIcon />
        Keyboard shortcuts
        <CommandShortcut>?</CommandShortcut>
      </CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

### Groups

A command menu with groups, icons and separators.

```astro live
---
import BoltIcon from "lucide-static/icons/bolt.svg"
import CalendarIcon from "lucide-static/icons/calendar.svg"
import CreditCardIcon from "lucide-static/icons/credit-card.svg"
import FileTextIcon from "lucide-static/icons/file-text.svg"
import FolderIcon from "lucide-static/icons/folder.svg"
import SettingsIcon from "lucide-static/icons/settings.svg"
import UserIcon from "lucide-static/icons/user.svg"

import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
} from "@/components/ui/command"
---

<Command class="w-full max-w-md ring-1 ring-foreground/10">
  <CommandInput placeholder="Search actions..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>
        <CalendarIcon />
        Calendar
      </CommandItem>
      <CommandItem>
        <FileTextIcon />
        Notes
      </CommandItem>
      <CommandItem>
        <FolderIcon />
        Projects
      </CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Account">
      <CommandItem>
        <UserIcon />
        Profile
      </CommandItem>
      <CommandItem>
        <CreditCardIcon />
        Billing
      </CommandItem>
      <CommandItem>
        <SettingsIcon />
        Settings
      </CommandItem>
      <CommandItem>
        <BoltIcon />
        API Keys
      </CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

### Scrollable

Scrollable command menu with multiple items.

```astro live
---
import ClockIcon from "lucide-static/icons/clock.svg"

import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
} from "@/components/ui/command"

const recentSearches = Array.from({ length: 18 }, (_, index) => ({
  label: `Recent search ${index + 1}`,
  value: `recent-search-${index + 1}`,
}))
---

<Command class="h-[24.5rem] w-full max-w-md ring-1 ring-foreground/10">
  <CommandInput placeholder="Search recent activity..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Recent">
      {
        recentSearches.map((item) => (
          <CommandItem value={item.value}>
            <ClockIcon />
            {item.label}
          </CommandItem>
        ))
      }
    </CommandGroup>
  </CommandList>
</Command>
```

## RTL

Apply `dir="rtl"` to a parent container to render the menu in right-to-left
mode.

```astro live
---
import BookmarkIcon from "lucide-static/icons/bookmark.svg"
import CalendarIcon from "lucide-static/icons/calendar.svg"
import StarIcon from "lucide-static/icons/star.svg"

import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
} from "@/components/ui/command"
---

<div dir="rtl">
  <Command class="h-[24.5rem] w-full max-w-md ring-1 ring-foreground/10">
    <CommandInput placeholder="ابحث عن أمر..." />
    <CommandList>
      <CommandEmpty>لا توجد نتائج.</CommandEmpty>
      <CommandGroup heading="اقتراحات">
        <CommandItem>
          <CalendarIcon />
          التقويم
        </CommandItem>
        <CommandItem>
          <StarIcon />
          المفضلة
        </CommandItem>
        <CommandItem>
          <BookmarkIcon />
          العلامات
        </CommandItem>
      </CommandGroup>
    </CommandList>
  </Command>
</div>
```

## API Reference

See the [`@data-slot/command`](https://www.npmjs.com/package/@data-slot/command)
documentation for runtime behavior, controller events, and supported data
attributes.
