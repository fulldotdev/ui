---
type: doc
title: Kbd
description: Used to display textual user input from keyboard.
---

```astro live props={{ name: 'kbd' }}
---
import { SearchIcon } from "@lucide/astro"

import { Button } from "@/components/ui/button"
import {
  InputGroup,
  InputGroupAddon,
  InputGroupInput,
} from "@/components/ui/input-group"
import { Kbd, KbdGroup } from "@/components/ui/kbd"
import {
  Tooltip,
  TooltipContent,
  TooltipTrigger,
} from "@/components/ui/tooltip"
---

<div class="flex w-full flex-col items-center gap-4">
  <KbdGroup>
    <Kbd>⌘</Kbd>
    <Kbd>K</Kbd>
  </KbdGroup>

  <Button variant="outline">
    <SearchIcon data-icon="inline-start" />
    Search
    <Kbd>⌘K</Kbd>
  </Button>

  <Tooltip>
    <TooltipTrigger as="span">
      <Button variant="outline">Keyboard shortcut</Button>
    </TooltipTrigger>
    <TooltipContent>
      Open command menu
      <Kbd>⌘K</Kbd>
    </TooltipContent>
  </Tooltip>

  <InputGroup class="max-w-sm">
    <InputGroupInput placeholder="Search docs..." />
    <InputGroupAddon align="inline-end">
      <Kbd>⌘K</Kbd>
    </InputGroupAddon>
  </InputGroup>
</div>
```

## Installation

```bash
npx shadcn@latest add @fulldev/kbd
```

## Usage

```ts
import { Kbd } from "@/components/ui/kbd"
```

```astro
<Kbd>Ctrl</Kbd>
```

## Composition

Use the following composition to build `Kbd` and `KbdGroup`:

```text
Kbd
KbdGroup
├── Kbd
└── Kbd
```

## Group

Use the `KbdGroup` component to group keyboard keys together.

```astro
---
import { Kbd, KbdGroup } from "@/components/ui/kbd"
---

<KbdGroup>
  <Kbd>Ctrl</Kbd>
  <Kbd>B</Kbd>
</KbdGroup>
```

## Notes

- Use `Kbd` for short key labels and keyboard shortcuts.
- Use `KbdGroup` when a shortcut has multiple separate keys.

## API Reference

See the [GitHub source code](https://github.com/fulldotdev/ui/tree/main/src/components/ui/kbd) for more information on props.
