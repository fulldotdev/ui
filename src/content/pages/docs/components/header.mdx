---
type: doc
title: Header
description: A responsive site header with grouped content.
---

## Installation

```bash
npx shadcn@latest add @fulldev/header
```

## Usage

```ts
import { Button } from "@/components/ui/button"
import { Header, HeaderContent, HeaderGroup } from "@/components/ui/header"
import { Logo, LogoText } from "@/components/ui/logo"
```

## Examples

### Basic Groups

```astro live
---
import { Button } from "@/components/ui/button"
import { Header, HeaderGroup } from "@/components/ui/header"
import { Logo, LogoText } from "@/components/ui/logo"
---

<Header>
  <HeaderGroup>
    <Logo href="/">
      <LogoText>Fulldev UI</LogoText>
    </Logo>
  </HeaderGroup>

  <HeaderGroup side="end">
    <Button size="sm" variant="ghost">Sign In</Button>
    <Button size="sm">Sign Up</Button>
  </HeaderGroup>
</Header>
```

### Floating

```astro live
---
import { Button } from "@/components/ui/button"
import { Header, HeaderGroup } from "@/components/ui/header"
import { Logo, LogoText } from "@/components/ui/logo"
---

<Header variant="floating">
  <HeaderGroup>
    <Logo href="/">
      <LogoText>Fulldev UI</LogoText>
    </Logo>
  </HeaderGroup>

  <HeaderGroup side="end">
    <Button size="sm" variant="ghost">Sign In</Button>
    <Button size="sm">Sign Up</Button>
  </HeaderGroup>
</Header>
```

## Notes

- `Header` controls the layout — `default` is full-width, `floating` is a constrained rounded card.
- `HeaderGroup` accepts `side="start | center | end"` to align its children inside the header row.
- `HeaderContent` is an optional middle slot when you need a flex region between left and right groups.
- For a mobile menu, use `Sheet` and `SheetTrigger` from `@/components/ui/sheet` directly inside a `HeaderGroup`.
