---
type: doc
title: Footer
description: A layout component for the bottom section of a page.
---

import { AlertCircle } from "@lucide/astro"

import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"

<Alert class="not-content">
  <AlertCircle />
  <AlertTitle>Work in progress</AlertTitle>
  <AlertDescription>
    This footer component is still being documented. Installation is stable, but
    the example coverage is not complete yet.
  </AlertDescription>
</Alert>

## Installation

To install the footer component, run the following command:

```bash
npx shadcn@latest add @fulldev/footer
```
