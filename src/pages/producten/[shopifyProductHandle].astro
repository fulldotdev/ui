<!-- ---
import { Product1 } from "@/blocks/product-1"
import ShopifyLayout from "@/layouts/shopify-layout.astro"

import { getProduct } from "@/lib/shopify"

export const prerender = false

const { shopifyProductHandle } = Astro.params
if (!shopifyProductHandle) return Astro.redirect("/404")

const product = await getProduct(shopifyProductHandle)
if (!product) return Astro.redirect("/404")

const { title, image, seo, body, ...rest } = product
---

<ShopifyLayout title={title} image={image} {...seo}>
  <Product1 client:load title={title} {...rest}>
    <Fragment set:html={body} />
  </Product1>
</ShopifyLayout> -->
