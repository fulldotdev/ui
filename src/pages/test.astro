---
import { getCollection, getEntry } from "astro:content"

import { transformData, transformPage } from "@/lib/transforms"

// const pages = await getCollection("pages")
const indexPage = await getEntry("pages", "index")
// const transformedPages = await transformPage.parseAsync(indexPage)
const transformedData = await transformData.parseAsync(indexPage?.data)

console.log(transformedData)
---

<pre>{JSON.stringify(transformedData, null, 2)}</pre>
