---
import { getEntries, getEntry } from 'astro:content'
import { CategoriesCard } from 'src/components/blocks/categories'
const test = await getEntry('test', 'test')
const entry = await getEntries(test.data.products)
console.log(entry)
---

{JSON.stringify(test)}
<!-- <CategoriesCard
  heading="hello there"
  query="woonkamer"
/> -->
