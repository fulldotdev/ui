<!-- ---
import { Collection1 } from "@/blocks/collection-1.tsx"
import ShopifyLayout from "@/layouts/shopify-layout.astro"

import { getCollection } from "@/lib/shopify"

export const prerender = false

const { shopifyCollectionHandle } = Astro.params
if (!shopifyCollectionHandle) return Astro.redirect("/404")

const collection = await getCollection(shopifyCollectionHandle)
if (!collection) return Astro.redirect("/404")

const { title, image, seo, body, ...rest } = collection
---

<ShopifyLayout title={title} image={image} {...seo}>
  <Collection1 client:load title={title} {...rest}>
    <Fragment set:html={body} />
  </Collection1>
</ShopifyLayout> -->
