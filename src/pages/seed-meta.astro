---
import Shopify from "shopify-api-node"

const shopify = new Shopify({
  shopName: "cardgamelife",
  apiKey: import.meta.env.SHOPIFY_ADMIN_KEY,
  password: import.meta.env.SHOPIFY_ADMIN_SECRET_KEY,
})

const metafields = await shopify.metafield.list()
---
