---
import { execSync } from "child_process"

// Simple server-side GitHub push example
if (Astro.request.method === "POST") {
  try {
    const formData = await Astro.request.formData()
    const action = formData.get("action")

    if (action === "push") {
      // Add all changes
      execSync("git add .", { stdio: "inherit", cwd: process.cwd() })

      // Commit with timestamp
      const timestamp = new Date().toISOString()
      execSync(`git commit -m "CMS update: ${timestamp}"`, {
        stdio: "inherit",
        cwd: process.cwd(),
      })

      // Push to GitHub (make sure you're on the right branch)
      execSync("git push origin HEAD", { stdio: "inherit", cwd: process.cwd() })

      console.log("Successfully pushed to GitHub!")
    }
  } catch (error) {
    console.error("Error pushing to GitHub:", error)
  }
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Simple CMS - Push to GitHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen bg-gray-100 p-8">
    <div class="mx-auto max-w-2xl">
      <h1 class="mb-8 text-3xl font-bold text-gray-900">Simple CMS</h1>

      <div class="mb-6 rounded-lg bg-white p-6 shadow-md">
        <h2 class="mb-4 text-xl font-semibold">Push Changes to GitHub</h2>
        <p class="mb-4 text-gray-600">
          This will add, commit, and push all changes in your repository to
          GitHub.
        </p>

        <form method="POST" class="space-y-4">
          <button
            type="submit"
            name="action"
            value="push"
            class="rounded-md bg-blue-600 px-4 py-2 font-medium text-white transition-colors hover:bg-blue-700"
          >
            Push to GitHub
          </button>
        </form>
      </div>

      <div class="rounded-lg border border-yellow-200 bg-yellow-50 p-4">
        <h3 class="mb-2 font-medium text-yellow-800">Setup Required:</h3>
        <ol class="space-y-1 text-sm text-yellow-700">
          <li>1. Make sure git is configured with your credentials</li>
          <li>2. Ensure you're on the correct branch</li>
          <li>3. Have push permissions to the repository</li>
          <li>4. Consider using environment variables for sensitive data</li>
        </ol>
      </div>

      <div class="mt-6 rounded-lg border border-gray-200 bg-gray-50 p-4">
        <h3 class="mb-2 font-medium text-gray-800">Git Commands Executed:</h3>
        <pre
          class="rounded border bg-white p-2 text-sm text-gray-600"><code>git add .
git commit -m "CMS update: [timestamp]"
git push origin HEAD</code></pre>
      </div>
    </div>
  </body>
</html>
