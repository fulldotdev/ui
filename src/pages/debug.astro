---
import { getEntry } from 'astro:content'
import { Heading } from 'src'
import { withDefaults } from 'src/utils'

const pages = await getEntry('pages', 'index')
const pagesWithDefaults = await withDefaults(pages)
---

<Heading
  heading={{
    heading: 'lorem ipsum',
  }}
  _heading="test"
  class="hello"
/>
