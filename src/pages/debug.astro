---
import {
  getCollection,
  getLayout,
  getPage,
  getProduct,
  getSearch,
} from "@/adapters/shopify/api"

import "@/styles/globals.css"

const product = await getProduct("gem-booster-box-s-chinese")
// const collection = await getCollection("disney")
// const page = await getPage("b2b")
// const layout = await getLayout()
// const search = await getSearch()

console.log({ product })
---

<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
</html>
