---
import type { ComponentProps } from 'astro/types'
import { CardsItems, CardsRoot } from '.'

interface Props
  extends ComponentProps<typeof CardsRoot>,
    ComponentProps<typeof CardsItems> {}

const { props } = Astro
---

<CardsRoot {...props}>
  <CardsItems {...props} />
</CardsRoot>
