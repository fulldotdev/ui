---
import { getSlot } from 'src/utils'

interface Props {
  data?: any
  render?: any
  class?: any
  collection?: any
}

const {} = Astro.props

const slot = await getSlot(Astro)
---

<!-- TODO: 1 and 2 columns -->{
  slot && (
    <article class:list={['full-theme', 'article']}>
      <Fragment set:html={slot} />
    </article>
  )
}

<style>
  article {
  }
</style>
