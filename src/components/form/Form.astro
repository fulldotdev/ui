---
import type { ComponentProps } from 'astro/types'
import FormItems from './FormItems.astro'
import FormRoot from './FormRoot.astro'
// import { Select } from '../select'

// interface Props {
//   form?: string | null
//   title?: HTMLAttributes<'input'>['value'] | null
//   key?: HTMLAttributes<'input'>['value'] | null
//   inputs?: (InputProps | SelectProps | textareaProps)[] | null
//   button?: ButtonProps | null
// }

// const components: any = {
//   input: Input,
//   textarea: textarea,
//   select: Select,
// }

interface Props
  extends ComponentProps<typeof FormRoot>,
    ComponentProps<typeof FormItems> {}

// let {
//   form,
//   title,
//   key,
//   inputs,
//   button = {
//     label: 'Verstuur',
//     href: '/bericht-ontvangen',
//   },
// } = Astro.props

const { props } = Astro

// const formName = form?.split('/').pop()?.replace('.md', '') || 'myplaceholder'
// const formEntry = await getEntry('forms', formName)
// const formData = formEntry ? formEntry.data : Astro.props

// const inboxKey = import.meta.env?.PUBLIC_APP_URL?.replace('https://', '')
//   ?.replace('http://', '')
//   ?.split('.')
//   ?.shift()
---

<FormRoot {...props}>
  <FormItems {...props} />
</FormRoot>
