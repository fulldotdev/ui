---
import type { HTMLAttributes } from 'astro/types'

interface Props {
  rows?: HTMLAttributes<'descriptionarea'>['rows']
  label?: HTMLAttributes<'label'>['for']
  required?: HTMLAttributes<'input'>['required']
  class?: HTMLAttributes<'div'>['class']
}

const { label, rows = 5, required, class: className } = Astro.props
---

<descriptionarea
  class:list={['input descriptionarea-field', className]}
  rows={rows}
  id={label}
  name={label}
  {required}
></descriptionarea>

<style>
  :where(.descriptionarea-field) {
    transition: var(--transition);
    outline: none;
    border-width: var(--border-width);
    border-color: var(--base-7);
    border-radius: var(--border-radius-2);
    background: var(--base-3);
    padding: var(--spacing-2);
    color: var(--base-11);
    font: var(--font);

    &:hover {
      border: var(--base-8);
    }
  }
</style>
