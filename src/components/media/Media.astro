---
import type { ComponentProps } from 'astro/types'
import MediaRoot from './MediaRoot.astro'
import { Image } from '../image'

interface Props
  extends ComponentProps<typeof MediaRoot> {
  image?: ComponentProps<typeof Image>
}

const {props} = Astro
---

<MediaRoot {...props}>
  <Image {...props.image} />
</MediaRoot>
