---
import { Buttons } from '@silveltman/fullui/src/components/buttons'
import { Cards } from '@silveltman/fullui/src/components/cards'
import { Writeup } from '@silveltman/fullui/src/components/writeup'
import type { ComponentProps } from 'astro/types'
import { DeckRoot, DeckSegment } from '.'

interface Props
  extends ComponentProps<typeof DeckRoot>,
    ComponentProps<typeof DeckSegment>,
    ComponentProps<typeof Writeup>,
    ComponentProps<typeof Buttons>,
    ComponentProps<typeof Cards> {}

const { props } = Astro
---

<DeckRoot {...props}>
  <DeckSegment {...props}>
    <Writeup {...props}>
      <slot name="writeup" />
    </Writeup>
    <Buttons {...props} />
    <slot name="segment" />
  </DeckSegment>
  <Cards {...props} />
</DeckRoot>
