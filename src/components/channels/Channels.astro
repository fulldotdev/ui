---
import type { ComponentProps } from 'astro/types'
import { Channel } from '../channel'

interface Props {
  channels?: ComponentProps<typeof Channel>[]
}

const { channels } = Astro.props
---

{channels?.map((channel, i) => <Channel {...channel} />)}
