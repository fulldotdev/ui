---
import { Heading } from '@silveltman/fullui/components/heading'
import { Tagline } from '@silveltman/fullui/components/tagline'
import { Text } from '@silveltman/fullui/components/text'
import type { ComponentProps } from 'astro/types'
import { WriteupRoot } from '.'

interface Props
  extends ComponentProps<typeof WriteupRoot>,
    ComponentProps<typeof Tagline>,
    ComponentProps<typeof Heading>,
    ComponentProps<typeof Text> {}

const { props } = Astro
---

<WriteupRoot {...props}>
  <Tagline {...props} />
  <Heading {...props} />
  <Text {...props} />
  <slot />
</WriteupRoot>
