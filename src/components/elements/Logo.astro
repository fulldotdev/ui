---
export interface Props {
  src?: string | null
  alt?: string | null
  href?: string
  class?: string
}

const { src, alt, href = '/' } = Astro.props
---

{
  src && (
    <a
      href={href}
      class:list={[Astro.props.class]}
    >
      <img
        src={src}
        alt={alt}
      />
      <slot />
    </a>
  )
}

<style>
  a > img {
    color: var(--text);
    height: 2em;
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
  }
</style>
