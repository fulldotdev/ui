---
import type { Props as SpecProps } from './Spec.astro'
import Spec from './Spec.astro'

interface Props extends Omit<SpecProps, 'label'> {
  specs?: string[] | null
}

const { specs } = Astro.props
---

{specs?.map((spec) => <Spec {spec} />)}
