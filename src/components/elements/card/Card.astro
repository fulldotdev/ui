---
import type { Props as HeadingProps } from '../../elements/Heading.astro'
import type { Props as PriceProps } from '../../elements/Price.astro'
import Price from '../../elements/Price.astro'
import type { Props as RatingProps } from '../../elements/Rating.astro'
import Rating from '../../elements/Rating.astro'
import type { Props as TaglineProps } from '../../elements/Tagline.astro'
import Tagline from '../../elements/Tagline.astro'
import type { Props as TextProps } from '../../elements/Text.astro'
import Text from '../../elements/Text.astro'
import type { Props as ImageProps } from '../../elements/image/Image.astro'
import Image from '../../elements/image/Image.astro'
import type { Props as SpecsProps } from '../../elements/spec/Specs.astro'
import Specs from '../../elements/spec/Specs.astro'
import Subheading from '../Subheading.astro'
import type { Props as ButtonGroupProps } from '../button/ButtonGroup.astro'
import ButtonGroup from '../button/ButtonGroup.astro'
import type { Props as CardRootProps } from './CardRoot.astro'
import CardRoot from './CardRoot.astro'
import type { Props as CardSegmentProps } from './CardSegment.astro'
import CardSegment from './CardSegment.astro'
import type { Props as CardWriteupProps } from './CardWriteup.astro'
import CardWriteup from './CardWriteup.astro'

export interface Props
  extends CardRootProps,
    CardSegmentProps,
    RatingProps,
    CardWriteupProps,
    TaglineProps,
    HeadingProps,
    TextProps,
    PriceProps,
    SpecsProps,
    ButtonGroupProps {
  image?: ImageProps | null
}

const { props } = Astro
---

<CardRoot {...props}>
  <Image {...props.image} />
  <slot />
  <CardSegment {...props}>
    <Rating {...props} />
    <!-- TODO: add icon component -->
    <CardWriteup {...props}>
      <Tagline {...props} />
      <Subheading {...props} />
      <Text {...props} />
      <slot name="writeup" />
    </CardWriteup>
    <Price {...props} />
    <Specs {...props} />
    <ButtonGroup {...props} />
    <slot name="segment" />
  </CardSegment>
</CardRoot>
