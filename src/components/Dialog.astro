---
import Button from './Button.astro'
import Card from './Card.astro'

const { name = 'dialog', button, ...rest } = Astro.props
---

<Button
  {...button}
  onClick={() => document.getElementById(name)?.showModal()}
/>

<Card
  as="dialog"
  id={name}
  {...rest}
/>
