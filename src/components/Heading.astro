---
import type { HTMLAttributes, HTMLTag } from 'astro/types'
import Element from 'fulldev-ui/components/Element.astro'
import type { GlobalProps } from 'fulldev-ui/types/global-props'

interface Props extends GlobalProps, HTMLAttributes<'h2'> {
  as?: HTMLTag
  level?: 1 | 2 | 3 | 4 | 5 | 6 | undefined
  text?: string | undefined
}

const { size = 'md', level = 2, as = `h${level}`, text, ...rest } = Astro.props
---

<Element
  class:list="heading"
  {as}
  {level}
  {size}
  {...rest}
>
  <slot />
  <Fragment set:html={text} />
</Element>

<style is:global>
  .heading {
    @apply mt-8 scroll-mt-20 font-bold tracking-tight first:mt-0;

    &.size-sm {
      &h1 {
        @apply text-3xl;
      }

      &h2 {
        @apply text-2xl;
      }

      &h3 {
        @apply text-xl;
      }

      &h4 {
        @apply text-lg;
      }

      &h5 {
        @apply text-base;
      }

      &h6 {
        @apply text-sm;
      }
    }

    &.size-md {
      &h1 {
        @apply text-4xl;
      }

      &h2 {
        @apply text-3xl;
      }

      &h3 {
        @apply text-2xl;
      }

      &h4 {
        @apply text-xl;
      }

      &h5 {
        @apply text-lg;
      }

      &h6 {
        @apply text-base;
      }
    }

    &.size-lg {
      &h1 {
        @apply text-5xl lg:text-6xl;
      }

      &h2 {
        @apply text-4xl;
      }

      &h3 {
        @apply text-3xl;
      }

      &h4 {
        @apply text-2xl;
      }

      &h5 {
        @apply text-xl;
      }

      &h6 {
        @apply text-lg;
      }
    }
  }
</style>
