---
import type { HTMLAttributes } from 'astro/types'
import { type VariantProps } from 'class-variance-authority'
import Element from 'fulldev-ui/components/Element.astro'
import { badgeVariants } from 'fulldev-ui/components/shadcn/badge/index.ts'

interface Props extends HTMLAttributes<'a'>, HTMLAttributes<'div'>, VariantProps<typeof badgeVariants> {
  text?: string | undefined
  href?: string | undefined
}

const { href, target = href?.startsWith('http') ? '_blank' : undefined, variant, text, ...rest } = Astro.props
---

<Element
  class:list={['badge', badgeVariants({ variant })]}
  as={href ? 'a' : 'div'}
  {href}
  {target}
  {...rest}
>
  <Fragment set:html={text} />
  <slot />
</Element>
