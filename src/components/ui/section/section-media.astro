---
import type { HTMLAttributes } from "astro/types"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

interface Props extends HTMLAttributes<"div">, VariantProps<typeof variants> {}

const variants = cva(
  [
    "relative size-full overflow-hidden *:size-full *:object-cover max-h-[60vh] flex items-center justify-center",
  ],
  {
    variants: {
      variant: {
        default: "*:rounded-lg",
        background: "absolute inset-0 size-full opacity-25",
        inset: [
          "-mx-(--section-gutter) w-[calc(100%+2*var(--section-gutter))]",
          "-my-(--section-spacer) h-[calc(100%+2*var(--section-spacer))]",
        ],
      },
      order: {
        default: "",
        reverse: "flex-col-reverse",
      },
      align: {
        start: "items-start",
        center: "items-center",
        end: "items-end",
      },
    },
    defaultVariants: {
      variant: "default",
      order: "default",
    },
  }
)

const { class: className, variant, order, ...props } = Astro.props

const slot = await Astro.slots.render("default")
---

{
  slot.trim().length > 0 && (
    <div
      data-slot="section-media"
      data-variant={variant}
      class={cn(variants({ variant, order }), className)}
      {...props}
    >
      <Fragment set:html={slot} />
    </div>
  )
}
