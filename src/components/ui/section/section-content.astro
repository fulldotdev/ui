---
import type { HTMLAttributes } from "astro/types"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

interface Props extends HTMLAttributes<"div">, VariantProps<typeof variants> {}

const variants = cva(
  [
    "relative px-4 lg:px-8 xl:px-16 py-16 flex flex-col gap-16 mx-auto max-w-screen-xl w-full",
  ],
  {
    variants: {
      order: {
        default: "",
        reverse: "flex-col-reverse",
      },
      align: {
        start: "items-start",
        center: "items-center",
        end: "items-end",
      },
    },
    defaultVariants: {
      order: "default",
    },
  }
)

const { class: className, order, ...props } = Astro.props

const slot = await Astro.slots.render("default")
---

{
  slot.trim().length > 0 && (
    <div
      data-slot="section-content"
      class={cn(variants({ order }), className)}
      {...props}
    >
      <Fragment set:html={slot} />
    </div>
  )
}
