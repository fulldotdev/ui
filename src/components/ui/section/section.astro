---
import type { HTMLAttributes } from "astro/types"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

type Props = HTMLAttributes<"section"> & VariantProps<typeof variants>

const variants = cva(
  "relative flex scroll-mt-12 flex-col gap-16 lg:scroll-mt-16",
  {
    variants: {
      variant: {
        default: "bg-background w-full",
        floating:
          "bg-background max-w-container mx-auto my-12 w-[calc(100%-3rem)] overflow-hidden rounded-xl border shadow-md lg:my-16",
      },
      size: {
        sm: "py-8 lg:py-12",
        default: "py-12 lg:py-16",
        lg: "py-24 lg:py-32",
      },
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    },
  }
)

const { class: className, variant, size, ...props } = Astro.props
---

<section
  class={cn(variants({ variant, size }), className)}
  data-slot="section"
  data-variant={variant}
  data-size={size}
  {...props}
>
  <slot />
</section>
