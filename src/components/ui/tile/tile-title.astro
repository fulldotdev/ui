---
import type { HTMLAttributes } from "astro/types"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"

interface Props extends HTMLAttributes<"h3">, VariantProps<typeof variants> {}

const variants = cva("leading-none font-semibold", {
  variants: {
    align: {
      start: "text-start",
      center: "text-center",
      end: "text-end",
    },
  },
  defaultVariants: {
    align: "start",
  },
})

const { class: className, align, ...props } = Astro.props

const slot = await Astro.slots.render("default")
---

{
  slot.trim().length > 0 && (
    <h3 class={cn(variants({ align }), className)} {...props}>
      <Fragment set:html={slot} />
    </h3>
  )
}
