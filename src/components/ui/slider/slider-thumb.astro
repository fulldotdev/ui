---
import type { HTMLAttributes } from "astro/types"

import { cn } from "@/lib/utils"

interface Props extends HTMLAttributes<"div"> {
  label?: string
}

const { class: className, label, ...props } = Astro.props
---

<div
  data-slot="slider-thumb"
  data-label={label}
  class={cn(
    "block size-4 shrink-0 rounded-full border border-primary bg-white shadow-sm ring-ring/50 transition-[color,box-shadow] select-none hover:ring-4 data-[disabled]:pointer-events-none data-[disabled]:opacity-50 data-[dragging]:shadow-md focus-visible:ring-4 focus-visible:outline-hidden",
    className
  )}
  {...props}
>
  <slot />
</div>
