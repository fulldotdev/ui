---
import type { HTMLAttributes } from "astro/types"
import { cva, type VariantProps } from "class-variance-authority"

import { cn } from "@/lib/utils"
import { BannerClose } from "@/components/ui/banner"

interface Props
  extends HTMLAttributes<"header">,
    VariantProps<typeof variants> {}

const variants = cva(
  [
    "relative mx-auto flex min-h-10 items-center justify-between gap-8 py-1",
    "border border-transparent",
  ],
  {
    variants: {
      frame: {
        // Filled on every screen, with content width based on padding
        default: [
          "bg-primary text-primary-foreground w-full px-[max(24px,calc((100cqw-var(--banner-width))/2))]",
        ],
        // Contained on every screen, with a border and padding for box effect
        box: [
          "w-[calc(100%-16px)] max-w-(--banner-width) px-4 lg:w-[calc(100%-48px)]",
          "overflow-hidden",
          "border-border bg-primary text-primary-foreground border",
        ],
        // Contained on every screen, with a border, padding and rounded corners for panel effect
        panel: [
          "w-[calc(100%-16px)] max-w-(--banner-width) px-4 lg:w-[calc(100%-48px)]",
          "my-2 overflow-hidden rounded-xl",
          "bg-primary text-primary-foreground border-border border",
        ],
      },
      variant: {
        default: "",
        secondary: "bg-secondary text-secondary-foreground",
        muted: "bg-muted border-transparent",
        outline: "border-border bg-background border",
      },
    },
    defaultVariants: {
      frame: "default",
      variant: "default",
    },
  }
)

const { class: className, frame, variant, ...props } = Astro.props

const slot = await Astro.slots.render("default")
---

<script is:inline>
  // Inline because it needs to run before the banner is rendered
  if (sessionStorage.getItem("closedBanners")) {
    document.body.classList.add("banner-hidden")
  } else {
    document.body.classList.remove("banner-hidden")
  }
</script>

{
  slot?.trim().length > 0 && (
    <banner
      class={cn(variants({ frame, variant }), className)}
      data-slot="banner"
      data-frame={frame}
      {...props}
    >
      <Fragment set:html={slot} />
    </banner>
  )
}

<script>
  document.querySelectorAll("[data-slot='banner-close']").forEach((btn) => {
    btn.addEventListener("click", () => {
      console.log("clicked")
      sessionStorage.setItem("closedBanners", "true")
      document.body.classList.add("banner-hidden")
    })
  })
</script>

<style is:global>
  body.banner-hidden [data-slot="banner"] {
    display: none;
  }

  [data-slot="banner"] {
    --banner-width: var(--container, var(--breakpoint-xl));
  }
</style>
