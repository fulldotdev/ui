---
import type { HTMLAttributes } from 'astro/types'

interface Props {
  class?: HTMLAttributes<'div'>['class']
  scheme?: 'light' | 'dark'
}

const { scheme, id, class: className } = Astro.props
---

<body class:list={['layout__body', scheme, className]} {id}>
  <slot />
</body>

<style>
  body {
    background: var(--base-1);
    min-height: 100vh;
  }

  .dark {
    color-scheme: dark;
  }

  .light {
    color-scheme: light;
  }

  :root {
    transition: var(--transition);
    background: var(--background);
    color: var(--color);
    font: var(--font);
  }
</style>
