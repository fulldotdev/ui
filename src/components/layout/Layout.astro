---
import CtaSection from '../../blocks/cta/CtaSection.astro'
import Whatsapp from '../Bubble.astro'
import Banner from '../banner/Banner.astro'
import Footer from '../footer/Footer.astro'
import Header from '../header/Header.astro'
import LayoutBody from './LayoutBody.astro'
import LayoutHead from './LayoutHead.astro'
import LayoutMain from './LayoutMain.astro'
import LayoutRoot from './LayoutRoot.astro'

const { props } = Astro
---

<LayoutRoot {...props}>
  <LayoutHead {...props}>
    <slot name="head" />
  </LayoutHead>
  <LayoutBody {...props}>
    <!-- <Banner
      impact="medium"
      scheme="dark"
      text="De goedkoopste van Nederland"
    /> -->
    <!-- <Header {...props}>
      <slot name="header" />
    </Header> -->
    <LayoutMain {...props}>
      <slot />
      <!-- <CtaSection {...props} /> -->
      <!-- <Whatsapp {...props} /> -->
    </LayoutMain>
    <!-- <Footer>
          <slot name="footer" />
        </Footer> -->
  </LayoutBody>
</LayoutRoot>
