---
import { Spec } from '@silveltman/fullui/components/spec'
import { getSlot } from '@silveltman/fullui/utils'

interface Props {
  specs?: string[]
}

const { specs } = Astro.props
const slot = getSlot(Astro)
---

{
  specs && (
    <ul>
      {specs?.map((spec) => (
        <Spec set:html={spec} />
      ))}
    </ul>
  )
}

<style>
  ul {
    display: flex;
    flex-direction: column;
    gap: var(--flow1);
  }
</style>
