---
import { type HeaderSchema } from "@/schemas/blocks/header"
import Header1 from "@/components/blocks/header-1"

type Props = HeaderSchema

const { variant = "1", ...block } = Astro.props
---

{variant === "1" && <Header1 client:load {...block} />}
