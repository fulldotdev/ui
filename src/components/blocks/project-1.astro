---
import { Button } from "@/components/ui/button"
import { Icon } from "@/components/ui/icon"
import { Image } from "@/components/ui/image"
import {
  Section,
  SectionActions,
  SectionContent,
  SectionGrid,
  SectionProse,
} from "@/components/ui/section"

interface Props {
  id?: string
  title?: string
  description?: string
  links?: {
    icon?: string
    text?: string
    href?: string
    target?: string
  }[]
  image?: {
    src: string
    alt: string
    title?: string
  }
}

const { id, title, description, links, image } = Astro.props
---

<Section id={id}>
  <SectionContent>
    <SectionProse title={title} description={description} />
    <SectionActions>
      {
        links?.map(({ icon, text, ...button }, i) => (
          <Button variant={i === 0 ? "default" : "outline"} {...button}>
            {icon && <Icon name={icon} />}
            {text}
          </Button>
        ))
      }
    </SectionActions>
  </SectionContent>
  <SectionGrid>
    <Image {...image} />
    <Image {...image} />
    <Image {...image} />
  </SectionGrid>
  <SectionProse>
    <slot />
  </SectionProse>
</Section>
