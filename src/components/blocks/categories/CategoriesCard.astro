---
import type { ComponentProps } from 'astro/types'
import Card from '../../elements/card/Card.astro'

interface Props
  extends Pick<
    ComponentProps<typeof Card>,
    'href' | 'image' | 'tagline' | 'heading' | 'text'
  > {}

const { props } = Astro
---

<Card {...props} />
