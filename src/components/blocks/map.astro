---
import type { MapSchema } from "@/schemas/blocks/map"

// import Map1 from "@/components/blocks/map-1"

type Props = MapSchema

const { variant, html, ...schema } = Astro.props
---

<!-- {
  variant === "1" && (
    <Map1 client:only="react" {...schema}>
      <Fragment set:html={html} />
      <slot />
    </Map1>
  )
} -->x
