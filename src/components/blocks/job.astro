---
import type { JobSchema } from "@/schemas/blocks/job"
import Job1 from "@/components/blocks/job-1"

type Props = JobSchema

const { variant, ...schema } = Astro.props

const props = {
  ...schema,
}
---

{
  variant === "1" && (
    <Job1 {...props}>
      <slot />
    </Job1>
  )
}
