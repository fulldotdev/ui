---
import type { ComponentProps } from "astro/types"

import { cn } from "@/lib/utils"
import { Button } from "@/components/ui/button"
import {
  Section,
  SectionActions,
  SectionContent,
  SectionGroup,
  SectionSplit,
} from "@/components/ui/section"
import { Typography } from "@/components/ui/typography"

interface Props extends ComponentProps<typeof Section> {
  size?: ComponentProps<typeof Typography>["size"]
  buttons?: ComponentProps<typeof Button>[]
  src?: string
}

const { class: className, align, size, buttons, src, ...props } = Astro.props
---

<Section class={cn("", className)} {...props}>
  <SectionContent class="container">
    <SectionSplit align={align}>
      <SectionGroup align="start">
        <Typography size={size}>
          <slot />
        </Typography>
        <SectionActions>
          {
            buttons?.map((button, i) => (
              <Button
                variant={i === 0 ? "default" : "outline"}
                size={size}
                {...button}
              />
            ))
          }
        </SectionActions>
      </SectionGroup>
      <iframe
        src={src}
        width="640"
        height="480"
        class="bg-muted aspect-video h-full w-full overflow-hidden rounded-lg"
      ></iframe>
    </SectionSplit>
  </SectionContent>
</Section>
