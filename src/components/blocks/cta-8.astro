---
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Section, SectionContent } from "@/components/ui/section"

type BlockAction = {
  label: string
  href: string
  variant?: "default" | "outline" | "ghost" | "secondary"
}

interface Props {
  props: {
    badge: string
    title: string
    description: string
    actions: [BlockAction, BlockAction]
  }
}

const { props } = Astro.props
---

<Section>
  <SectionContent class="items-center gap-6 text-center">
    <Badge variant="outline">{props.badge}</Badge>
    <h2 class="max-w-3xl text-3xl font-semibold tracking-tight text-balance sm:text-4xl">{props.title}</h2>
    <p class="text-muted-foreground max-w-2xl text-base leading-7">{props.description}</p>
    <div class="flex flex-wrap justify-center gap-3">
      {
        props.actions.map((action) => (
          <Button as="a" href={action.href} variant={action.variant}>
            {action.label}
          </Button>
        ))
      }
    </div>
  </SectionContent>
</Section>
