---
import { Avatar, AvatarImage } from "@/components/ui/avatar"
import { Button } from "@/components/ui/button"
import { Icon } from "@/components/ui/icon"
import { Image } from "@/components/ui/image"
import {
  Item,
  ItemContent,
  ItemDescription,
  ItemMedia,
} from "@/components/ui/item"
import { List, ListItem } from "@/components/ui/list"
import { Rating } from "@/components/ui/rating"
import {
  Section,
  SectionActions,
  SectionContent,
  SectionGrid,
  SectionMedia,
  SectionTypography,
} from "@/components/ui/section"
import {
  Tile,
  TileContent,
  TileMedia,
  TileTypography,
} from "@/components/ui/tile"

interface Props {
  list?: string[]
  buttons?: {
    icon?: string
    text?: string
    href?: string
    target?: string
  }[]
  image?: {
    src: string
    alt: string
    title?: string
  }
  item?: {
    avatars?: {
      src: string
      alt: string
      title?: string
    }[]
    rating?: number
    description?: string
  }
  items?: {
    title?: string
    description?: string
    html?: string
    icon?: string
    href?: string
    buttons?: {
      icon?: string
      text?: string
      href?: string
      target?: string
    }[]
  }[]
}

const { list, buttons, image, item, items } = Astro.props
---

<Section class="bg-background dark py-32 lg:py-48">
  <SectionContent class="relative z-10">
    <SectionTypography size="lg">
      <slot />
    </SectionTypography>
    <List>
      {
        list?.map((item) => (
          <ListItem>
            <Icon name="check" />
            {item}
          </ListItem>
        ))
      }
    </List>
    <SectionActions>
      {
        buttons?.map(({ icon, text, ...button }, i) => (
          <Button
            variant={i === 0 ? "default" : "secondary"}
            size="lg"
            {...button}
          >
            {icon && <Icon name={icon} />}
            {text}
          </Button>
        ))
      }
    </SectionActions>
    <Item class="-m-4">
      <ItemMedia class="-space-x-4">
        {
          item?.avatars?.map((avatar) => (
            <Avatar class="ring-background size-10 ring-2">
              <AvatarImage {...avatar} />
            </Avatar>
          ))
        }
      </ItemMedia>
      <ItemContent>
        <Rating rating={item?.rating} />
        <ItemDescription>
          {item?.description}
        </ItemDescription>
      </ItemContent>
    </Item>
  </SectionContent>
  <SectionMedia variant="background">
    <Image {...image} />
  </SectionMedia>
</Section>
