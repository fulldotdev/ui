---
import type { Props as CardProps } from '../../elements/card/CardOld.astro'
import Card from '../../elements/card/CardOld.astro'

export interface Props extends Pick<CardProps, 'tagline' | 'heading' | 'text'> {
  // 'pay-min'?: number
  // 'pay-max'?: number
  // hours?: number
}

const { props } = Astro
---

<Card {...props} />
