---
import LayoutAction from '@layouts/LayoutAction.astro'
import type { CollectionEntry } from 'astro:content'
import Prose from '../../elements/Prose.astro'
import Image from '../../elements/image/Image.astro'
// import Writeup from '../components/wrappers/Writeup.astro'
// import Block from '../components/wrappers/section/Section.astro'

interface Props {
  entry: CollectionEntry<'posts'>
}

const { data, render } = Astro.props.entry
const { Content } = await render()
---

<LayoutAction seo={data.seo}>
  <!-- <Block
    size="large"
    frame="float"
    class:list="!max-w-5xl lg:mx-auto !pt-flow5"
  >
    <Segment>
      <Writeup
        title={data.title}
        description={data.description}
      />
    </Segment>
    <Image {...data.image} />
  </Block>
  <Block
    align="center"
    frame="float"
    class:list="!max-w-4xl lg:mx-auto !pt-flow5 w-full"
  >
    <Prose Content={Content} />
  </Block> -->
</LayoutAction>
