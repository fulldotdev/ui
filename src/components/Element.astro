---
const { as: As = 'div', show = 'content', ...rest } = Astro.props

const slot = await Astro.slots.render('default')
const render = show === 'content' ? slot?.trim().length > 0 : show
---

{
  render && (
    <As {...rest}>
      <Fragment set:html={slot} />
    </As>
  )
}

<style is:global>
  @import 'fulldev-ui/styles/tailwind' layer(fulldev);
  @import 'fulldev-ui/styles/fulldev' layer(fulldev);
</style>
