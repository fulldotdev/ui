---
import type { ComponentProps, HTMLAttributes } from 'astro/types'
import Element from 'fulldev-ui/components/Element.astro'

interface Props extends ComponentProps<typeof Element>, HTMLAttributes<'form'> {
  structure?: 'column' | 'row' | 'group' | undefined
}

const { structure = 'column', ...rest } = Astro.props
---

<Element
  class:list={`form ${structure}`}
  as="form"
  {...rest}
>
  <slot />
</Element>

<style is:global lang="scss">
  .form {
    display: flex;
    width: 100%;
    max-width: calc(var(--space-4) * 44);
  }
</style>
