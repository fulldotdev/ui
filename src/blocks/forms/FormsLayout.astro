---
import type { CollectionEntry } from 'astro:content'
import { Layout } from '../../components/layout'
import { FormsSection } from '.'

interface Props {
  entry: CollectionEntry<'products'>
}

const { entry } = Astro.props
---

<Layout {...entry.data}>
  <FormsSection
    slug={entry.slug}
/>
  <slot />
</Layout>
