---
import MasterSection from 'fulldev-blocks/components/MasterSection.astro'

const {
  structure = 'column',
  size = 'xl',
  level = 1,
  align = 'center',
  ...rest
} = Astro.props
---

<MasterSection
  {structure}
  {size}
  {level}
  {align}
  {...rest}
/>
