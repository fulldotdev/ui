---
import Container from '@/components/Container.astro'
import Element from '@/components/Element.astro'
import Section from '@/components/Section.astro'
import Writeup from '@/components/Writeup.astro'

interface Props {
  title?: string | undefined
  description?: string | undefined
  content?: string | undefined
}

const { title, description, content } = Astro.props
---

<Section class="intro intro-1 pb-0 pt-0">
  <Container>
    <Element class="border-b border-border py-12">
      <Writeup
        align="start"
        size="sm"
      >
        <Element as="h1">{title}</Element>
        <Element as="p">{description}</Element>
        <Fragment set:html={content} />
        <slot />
      </Writeup>
    </Element>
  </Container>
</Section>
