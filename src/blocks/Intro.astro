---
import MasterSection from 'fulldev-ui/components/MasterSection.astro'

const { image, ...rest } = Astro.props
---

<MasterSection
  class="intro"
  structure="column"
  size="sm"
  level={1}
  variant="base"
  {...rest}
/>

<style is:global>
  .intro {
    border-bottom: 1px solid var(--base-6);
  }
</style>
