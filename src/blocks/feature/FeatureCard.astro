---
import type { ComponentProps } from 'astro/types'
import { Card } from '../../components'

interface Props
  extends Omit<ComponentProps<typeof Card>, 'collection' | 'slug'> {}

let { props } = Astro
---

<Card
  class="feature-card"
  {...props}
/>
