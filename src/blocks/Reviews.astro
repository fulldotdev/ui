---
import MasterSection from 'fulldev-ui/components/MasterSection.astro'

const {
  structure = 'masonry',
  align = 'center',
  frame = 'panel',
  variant = 'base',
  ...rest
} = Astro.props
---

<MasterSection
  class:list={'reviews'}
  {structure}
  {align}
  {frame}
  {variant}
  {...rest}
/>
