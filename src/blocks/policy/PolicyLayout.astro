---
import type { CollectionEntry } from 'astro:content'
import { PolicyArticle } from '.'
import { Layout } from '../../components'

interface Props {
  entry: CollectionEntry<'policies'>
}

const { entry } = Astro.props
---

<Layout {...entry.data}>
  <PolicyArticle {...entry} />
  <slot />
</Layout>
