---
import { Article } from '@silveltman/fullui/components'
import type { ComponentProps } from 'astro/types'

interface Props extends ComponentProps<typeof Article> {
  collection?: 'posts'
}

const { props } = Astro
---

<Article
  {...props}
  class="posts-article"
  collection="posts"
/>
