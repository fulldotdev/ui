---
import MasterSection from 'fulldev-ui/components/MasterSection.astro'

const { structure = 'grid', frame = 'panel', ...rest } = Astro.props
---

<MasterSection
  class:list={'features'}
  {structure}
  {frame}
  {...rest}
/>
