---
import type { CollectionEntry } from 'astro:content'
import { Blocks, Layout } from 'src/components'

interface Props {
  entry: CollectionEntry<'pages'>
}

const { entry } = Astro.props
---

<Layout
  {...entry.data}
  class="pages-layout"
>
  <Blocks {...entry.data} />
  <slot />
</Layout>
