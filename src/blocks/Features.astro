---
import MasterSection from 'fulldev-blocks/components/MasterSection.astro'

const { structure = 'grid', frame = 'fill', ...rest } = Astro.props
---

<MasterSection
  class:list={'features'}
  {structure}
  {frame}
  {...rest}
/>
