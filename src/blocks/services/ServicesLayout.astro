---
import type { CollectionEntry } from 'astro:content'
import { Layout, Renderer } from '../../components'

interface Props {
  entry: CollectionEntry<'services'>
}

const { entry } = Astro.props
---

<Layout
  {...entry.data}
  class="services-layout"
>
  <Renderer blocks={entry.data.blocks} />
  <slot />
</Layout>
