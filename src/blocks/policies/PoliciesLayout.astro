---
import { Layout } from '@silveltman/fullui/src/components'
import type { CollectionEntry } from 'astro:content'
import { PoliciesArticle } from '.'

interface Props {
  entry: CollectionEntry<'policies'>
}

const { entry } = Astro.props
---

<Layout {...entry.data}>
  <PoliciesArticle {...entry} />
  <slot />
</Layout>
