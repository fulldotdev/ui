---
import { Card } from '@silveltman/fullui/src/components'
import type { ComponentProps } from 'astro/types'

interface Props extends ComponentProps<typeof Card> {
  slug: never
  collection?: never
}

let { props } = Astro
---

<Card
  {...props}
  class="offers-card"
/>
