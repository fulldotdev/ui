---
import type { CollectionEntry } from 'astro:content'
import Intro from 'fulldev-ui/blocks/Intro.astro'
import PropsTable from 'fulldev-ui/components/docs/PropsTable.astro'
import Section from 'fulldev-ui/components/Section.astro'
import MainLayout from 'fulldev-ui/layouts/MainLayout.astro'

type Props = CollectionEntry<'pages'>

const { data, slug, render, ...rest } = Astro.props
const { Content } = await render()
---

<MainLayout
  {...Astro.props}
  class:list={['structure-layout']}
>
  <Intro
    variant="base"
    size="sm"
    {...data}
  />
  <Content />
  <Section>
    <PropsTable component={'structures/' + data.title} />
  </Section>
  <slot />
</MainLayout>

<style is:global lang="scss">
  .structure-layout {
    .example-container {
      display: flex;
      justify-content: center;
      align-items: center;
      padding: var(--space-5);
    }
  }
</style>
