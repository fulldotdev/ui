---
import type { ComponentProps } from 'astro/types'
import Box from 'fulldev-ui/components/Box.astro'
import Layout from 'fulldev-ui/components/Layout.astro'
import 'fulldev-ui/styles/global.css'

interface Props extends ComponentProps<typeof Layout> {}
---

<Layout>
  <Box as="main">
    <slot />
  </Box>
</Layout>
