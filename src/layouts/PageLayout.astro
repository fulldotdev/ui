---
import type { ComponentProps } from 'astro/types'
import Layout from 'fulldev-ui/layouts/Layout.astro'

type Props = ComponentProps<typeof Layout>

const { ...rest } = Astro.props
---

<Layout
  class:list="page-layout"
  {...rest}
>
  <slot />
</Layout>
