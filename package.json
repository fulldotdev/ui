{
  "name": "fullui-monorepo",
  "type": "module",
  "license": "Unlicense",
  "scripts": {
    "dev": "pnpm --filter demo dev",
    "check": "pnpm --filter demo check",
    "build": "pnpm --filter demo build",
    "preview": "pnpm --filter demo preview",
    "test": "pnpm --filter demo test",

    "demo:add": "pnpm add --filter demo",
    "package:add": "pnpm add --filter demo",
    
    "docs:add": "pnpm add --filter docs",
    "docs:dev": "pnpm --filter docs dev",
    "docs:check": "pnpm --filter docs check",
    "docs:build": "pnpm --filter docs build",
    "docs:preview": "pnpm --filter docs preview",
    "docs:test": "pnpm --filter docs test"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "prettier": "3.2.5",
    "prettier-plugin-astro": "0.13.0",
    "prettier-plugin-astro-organize-imports": "^0.4.3",
    "prettier-plugin-css-order": "^2.0.1",
    "prettier-plugin-organize-imports": "^3.2.4"
  }
}