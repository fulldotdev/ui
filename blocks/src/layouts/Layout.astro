---
import type { ComponentProps } from 'astro/types'
import { Layout as FullLayout } from 'fulldev-ui'

interface Props extends ComponentProps<typeof FullLayout> {}

const { ...rest } = Astro.props
---

<FullLayout
  lang="nl"
  {...rest}
>
  <slot />
</FullLayout>

<style is:global lang="scss"></style>
