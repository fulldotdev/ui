---
import type { ComponentProps } from 'astro/types'
import { Prose } from 'fulldev-ui'
import Blocks from '../components/Blocks.astro'
import Product from '../components/blocks/Product.astro'
import Layout from './Layout.astro'
import NavLayout from './NavLayout.astro'

interface Props extends ComponentProps<typeof Layout> {
  product?: any
  blocks?: any
}

const { product, blocks, ...rest } = Astro.props
---

<NavLayout {...rest}>
  <Product {...product} />
  <Prose>
    <slot name="content" />
  </Prose>
  <Blocks {...blocks} />
  <slot />
</NavLayout>

<style is:global lang="scss"></style>
