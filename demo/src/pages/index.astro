---
import { Page } from '@fulldevlabs/fullui';
import { getEntry } from 'astro:content';
const page = await getEntry('pages', 'index');
// console.log(JSON.stringify(page, null, 2));
---

<Page {...page.data} />
