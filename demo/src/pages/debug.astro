---
import { z } from 'astro:content'

const zodObejct = z.object({
  name: z.string(),
  age: z.number(),
  isHuman: z.boolean(),
  address: z.object({
    street: z.string(),
    city: z.string(),
    zip: z.string(),
  }),
})

console.log(typeof zodObejct)
---
