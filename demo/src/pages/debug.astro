---
import {
  Section,
  getEntriesByProps,
  getEntryByProps,
  type EntriesReference,
  type EntryReference,
} from '@fulldevlabs/fullui'

const props: EntryReference = {
  groepslessen: 'bbb',
}

const propss: EntriesReference = {
  groepslessen: ['bbb', 'ccc'],
}

const entry = await getEntryByProps(props)
const entries = await getEntriesByProps(props)

console.log('entry', entry)
console.log('entries', entries)
---

<Section />
