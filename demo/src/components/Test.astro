---
import type { HTMLAttributes } from 'astro/types'

type HTMLTag = 'div' | 'a'
interface Props<As extends HTMLTag> {
  as?: HTMLTag
  class?: HTMLAttributes<HTMLTag>['class']
  id?: HTMLAttributes<HTMLTag>['id']
  href?: As extends 'a' ? HTMLAttributes<'a'>['href'] : never
}
---
