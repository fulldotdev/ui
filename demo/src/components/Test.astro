---
import type { BuildProps } from '@fulldevlabs/fullui'
import type { HTMLTag } from 'astro/types'

interface Base {
  test?: string | null
}

type Props<Tag extends HTMLTag = 'a'> = BuildProps<Base, 'test', Tag>

const { as: Tag = 'a', ...props } = Astro.props
---

<Tag {...props} />
